import { RegisterForm } from '@/components/admin/AuthForm';

export default function RegisterPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4">
      <RegisterForm />
    </main>
  );
}
