// Preencha com os dados do seu projeto Supabase (Project Settings → API).
// A "anon key" é pública por design — é assim que o Supabase funciona, a
// segurança de verdade vem das políticas de Row Level Security no banco,
// não do sigilo desta chave. Por isso este arquivo NÃO precisa ficar fora
// do controle de versão.
window.SUPABASE_URL = 'https://SEU-PROJETO.supabase.co';
window.SUPABASE_ANON_KEY = 'sua-anon-key-publica';
